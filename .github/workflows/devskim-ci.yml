name: Guardian Security Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ Develop ]

jobs:
  security-scan:
    name: Security Testing
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup .NET for DevSkim
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'
          
      - name: Install DevSkim
        run: |
          dotnet tool install --global Microsoft.CST.DevSkim.CLI
          mkdir -p reports
        
      - name: Run DevSkim Scan
        continue-on-error: true
        run: |
          mkdir -p reports
          devskim analyze . \
            --severity-threshold moderate \
            --file-format json \
            --output-file reports/devskim-results.json
            
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Run Security Tests
        continue-on-error: true
        run: |
          pip install requests
          echo "  Skipping dynamic tests - DevSkim scan only"
          
      - name: Upload Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: reports/
          retention-days: 30
          
      - name: Check Results
        run: |
          if [ -f reports/devskim-results.json ]; then
            CRITICAL=$(jq '[.[] | select(.severity == "Critical")] | length' reports/devskim-results.json)
            if [ "$CRITICAL" -gt 0 ]; then
              echo " Found $CRITICAL critical vulnerabilities"
              exit 1
            fi
          fi
          echo " Security scan passed"